# The profile the test suite runs under (@ActiveProfiles("test")).
spring:
  datasource:
    url: jdbc:h2:mem:inventory-service-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  test:
    database:
      replace: none
